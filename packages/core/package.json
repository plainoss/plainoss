{
  "name": "@plainoss/core",
  "version": "0.1.0",
  "private": true,
  "description": "Pure calculation engines, algorithms, and math for PlainOSS tools",
  "type": "module",
  "license": "MIT",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./*": "./src/*.ts"
  },
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@plainoss/tsconfig": "workspace:*",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}
